// Selecionando todos os elementos usando querySelectorAll
const elementos = document.querySelectorAll('.container, .card, p, button');

// Mostrando no console o número de elementos
console.log(elementos);

// Selecionando o primeiro card
const elemento = document.querySelector('p');

elemento.innerText = "Produto 2"